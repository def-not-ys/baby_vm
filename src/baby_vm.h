#include <stdio.h>
#include <string.h>
#include <assert.h>

#include "types_private.h"

// memory model
ErrorStatus memory_load_instructions(FILE* ptr_file, Arguments* ptr_arg);

// hardware model

// process model